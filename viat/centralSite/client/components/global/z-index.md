# Z-index (global components)

Canonical scale and rules live next to the CSS token source:

→ **[`../core/styles/z-index.md`](../core/styles/z-index.md)**  
→ Values: `styles/variables.css` (`--z-*`)

Do not invent bare `z-index` integers for fixed/sticky chrome in `global/`.
